<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kairat.kairotube">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KairoTube">
        <activity android:name=".ui.activities.play.PlayActivity"></activity>
        <activity android:name=".ui.activities.playlist.PlaylistActivity" />
        <activity android:name=".ui.activities.tryconnection.TryConnectionActivity" />
        <activity android:name=".ui.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>